#include <iostream>
#include <iomanip>
using namespace std;
int main() {
    float number = 123.456789;
    cout<<fixed<<setprecision(2);
    cout<<"Formatted number (fixed-point): "<<number<<endl;
    cout<<setprecision(3);
    cout<<"Formatted number (scientific): "<<scientific<<number<<endl;
    cout<<setw(10)<<setfill('0');
    cout<<"Formatted number (width and filled): "<<number<<endl;
    return 0;
}
